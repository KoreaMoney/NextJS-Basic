import React from 'react';
import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'The record | Contact',
    description: 'Contact the record',
};

const Contact = () => {
    return <div>Contact Us!</div>;
};

export default Contact;
