import React from 'react';
import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'The record | About',
    description: 'About the record',
};

const About = () => {
    return <div>About Us!</div>;
};

export default About;
